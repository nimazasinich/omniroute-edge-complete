# V3.2 Acceptance Matrix

## Source/runtime gates

| Gate | Current evidence |
|---|---|
| Worker is sole production `/v1/*` path | PASS — contract/static tests |
| OmniRoute remains sole routing/fallback authority | PASS — contract/static tests |
| Bad auth rejected before origin | PASS — dependency-free gateway test |
| Missing rate-limit binding fails closed | PASS — dependency-free gateway test |
| Rate-limited request returns 429 without origin call | PASS — dependency-free gateway test |
| Origin requires HTTPS outside localhost | PASS — dependency-free gateway test |
| Client auth stripped before upstream | PASS — dependency-free gateway test |
| Streaming upstream body preserved | PASS — dependency-free gateway test |
| Network failure returns honest 502 with no edge fallback | PASS — dependency-free gateway test |
| Requested model recorded only from bounded real JSON body | PASS — dependency-free test |
| Provider/selected-model/tokens/cost are not fabricated | PASS — contract/static tests |
| D1 nullable observed token/cost schema | PASS — schema/migration/upgrade contract tests |

## UI truthfulness gates

| Gate | Current evidence |
|---|---|
| Approved dashboard hierarchy implemented | PASS — UI contract test |
| Screenshot sample metrics are not hard-coded | PASS — UI contract test |
| Provider/model views are read-only snapshots | PASS |
| Routing view does not reconstruct candidate scores | PASS |
| Security view does not claim a local prompt firewall | PASS |
| Logs use observed nullable token/cost fields | PASS |
| Traces show edge lifecycle facts, not scoring fiction | PASS |
| Missing alert API is distinct from real empty alerts | PASS |
| Header does not invent environment/operator identity | PASS |
| Sidebar does not present legacy provider snapshot as live gateway health | PASS |
| Topology feature badges reflect implemented edge capabilities | PASS |
| Nonfunctional time-range/search claims removed | PASS |
| Geographic map is labelled illustrative | PASS — source review/static text |

## Packaging/deployment gates

| Gate | Current evidence |
|---|---|
| No `.env`, DB snapshot, raw key export, `node_modules`, stale `dist`, `.wrangler` | PASS — static/package scan |
| Known token prefixes absent | PASS — static/package scan |
| Package/lock version and dependency roots consistent | PASS — static verifier |
| Deploy config fails closed while placeholders remain | PASS — behavioral test |
| Deploy config can pass a verified-shaped HTTPS/D1 config | PASS — behavioral test |
| `npm ci` | BLOCKED — online timeout; offline cache missing `yocto-queue-1.2.2.tgz` |
| `tsc --noEmit` with installed project dependencies | BLOCKED by dependency restore |
| Vitest | BLOCKED by dependency restore |
| Vite production build | BLOCKED by dependency restore |
| Fresh React runtime screenshot from V3.2 | BLOCKED by production build |
| Cloudflare D1/Tunnel/Access/live Worker verification | UNVERIFIED — requires target environment |
| Real OmniRoute routing/fallback evidence | UNVERIFIED — requires configured live OmniRoute |

`BLOCKED`, `SKIP`, and `UNVERIFIED` are never PASS.
